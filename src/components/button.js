import React from "react"
import Button from "react-bootstrap/Button"

import resume from '../assets/Eva_Huang_resume.pdf'
import containerStyles from "../styles/button.module.css"

export default ({  }) => (
    
    <Button className={containerStyles.btn} >Download resume</Button>

)